# 动态模型提供者实现方案

## 1. 需求分析

### 1.1 核心需求
- 在服务页面上动态添加新的 model provider
- 配置对应的 API key 和 URL 连接
- 通过请求接口动态查询当前所有模型列表
- 根据用户选择目标模型

### 1.2 技术背景
- 项目使用 Electron + React + TypeScript 架构
- 已实现基础的模型提供者架构，包括 `ModelProvider` 接口和 `GenericProvider` 类
- 已实现配置管理和 IPC 通信机制

## 2. 功能模块设计

### 2.1 后端模块

#### 2.1.1 模型提供者管理
- **ModelProviderRegistry**：支持运行时注册和管理自定义提供者
- **GenericProvider**：用于动态创建模型提供者实例
- **ModelConfigManager**：管理自定义提供者配置

#### 2.1.2 IPC 接口
- **get-custom-providers**：获取所有自定义提供者
- **add-custom-provider**：添加新的自定义提供者
- **remove-custom-provider**：删除自定义提供者
- **get-models**：根据提供者名称和 API key 获取模型列表

### 2.2 前端模块

#### 2.2.1 设置对话框扩展
- **自定义提供者管理界面**：添加、编辑、删除自定义提供者
- **动态模型列表查询**：根据选择的提供者和 API key 动态查询模型
- **模型选择界面**：根据查询结果展示模型列表供用户选择

#### 2.2.2 服务页面集成
- **提供者选择**：在服务页面上选择已配置的提供者
- **模型选择**：根据提供者动态加载模型列表

## 3. 实现方案

### 3.1 后端实现

#### 3.1.1 模型提供者注册
- 完善 `ModelProviderRegistry` 的 `registerCustomProvider` 方法
- 确保 `GenericProvider` 能够正确处理不同 API 格式的模型列表查询

#### 3.1.2 IPC 处理程序
- 确保现有的 IPC 处理程序能够正确处理自定义提供者的操作
- 优化 `get-models` 方法，确保能够处理不同提供者的 API 响应格式

### 3.2 前端实现

#### 3.2.1 设置对话框扩展
1. **添加自定义提供者管理界面**
   - 在 `SettingsDialog.tsx` 中添加自定义提供者管理区域
   - 实现添加、编辑、删除自定义提供者的功能
   - 提供 API key 和 URL 配置界面

2. **实现动态模型列表查询**
   - 添加 API key 验证和模型列表查询功能
   - 当用户输入 API key 后，自动查询并显示可用模型

3. **优化模型选择界面**
   - 支持从动态查询的模型列表中选择模型
   - 为自定义提供者添加模型选择逻辑

#### 3.2.2 服务页面集成
- 在服务页面上添加提供者选择下拉菜单
- 当选择提供者时，动态加载对应的模型列表
- 确保服务调用时使用正确的提供者和模型

## 4. 改动点

### 4.1 后端改动

1. **electron/ModelProviderRegistry.ts**
   - 完善 `registerCustomProvider` 方法
   - 确保能够正确管理自定义提供者的生命周期

2. **electron/providers/GenericProvider.ts**
   - 优化 `getModels` 方法，支持不同 API 格式
   - 增强错误处理和重试机制

3. **electron/ipcHandlers.ts**
   - 确保 IPC 处理程序能够正确处理自定义提供者的操作
   - 优化 `get-models` 方法的错误处理

### 4.2 前端改动

1. **src/components/Settings/SettingsDialog.tsx**
   - 添加自定义提供者管理界面
   - 实现动态模型列表查询功能
   - 优化模型选择逻辑

2. **src/types/electron.d.ts**
   - 完善自定义提供者相关的类型定义
   - 确保 TypeScript 类型安全

3. **服务页面组件**
   - 添加提供者选择功能
   - 集成动态模型列表加载

## 5. 技术实现细节

### 5.1 自定义提供者配置结构
```typescript
interface CustomProvider {
  name: string;           // 提供者唯一标识
  displayName: string;    // 显示名称
  apiKey: string;         // API 密钥
  baseUrl: string;        // API 基础 URL
  apiKeyPattern: string;  // API 密钥格式正则表达式
  defaultModels: {
    extraction: string;   // 默认提取模型
    solution: string;     // 默认解决方案模型
    debugging: string;    // 默认调试模型
  };
}
```

### 5.2 模型列表查询流程
1. 用户输入 API key 和 URL
2. 前端调用 `get-models` IPC 方法
3. 后端使用 `GenericProvider` 查询模型列表
4. 前端显示查询结果供用户选择

### 5.3 服务调用流程
1. 用户选择提供者和模型
2. 前端传递选择的提供者和模型到后端
3. 后端使用对应的提供者和模型执行服务

## 6. 测试计划

### 6.1 功能测试
- 测试添加、编辑、删除自定义提供者
- 测试不同 API key 和 URL 的模型列表查询
- 测试模型选择和服务调用

### 6.2 错误处理测试
- 测试无效 API key 的处理
- 测试无效 URL 的处理
- 测试网络错误的处理

### 6.3 性能测试
- 测试模型列表查询的响应时间
- 测试服务调用的性能

## 7. 实现优先级

1. **高优先级**：设置对话框的自定义提供者管理功能
2. **高优先级**：动态模型列表查询功能
3. **中优先级**：服务页面的提供者和模型选择集成
4. **低优先级**：性能优化和错误处理增强

## 8. 预期成果

- 用户可以在设置页面添加自定义模型提供者
- 用户可以配置 API key 和 URL 连接
- 系统能够动态查询并显示可用模型列表
- 用户可以在服务页面选择目标模型
- 系统能够使用选定的提供者和模型执行服务

## 9. 技术风险评估

### 9.1 潜在风险
- 不同 API 提供商的接口格式差异
- 网络请求失败和超时处理
- 前端状态管理复杂性

### 9.2 风险缓解策略
- 设计通用的 API 响应适配器
- 实现完善的错误处理和重试机制
- 使用状态管理库管理复杂状态

## 10. 总结

本方案通过扩展现有的模型提供者架构，实现了动态添加模型提供者和查询模型列表的功能。方案充分利用了现有的代码基础，只需要对前端界面和后端逻辑进行适当的扩展，即可满足需求。实现后，用户将能够更灵活地配置和使用不同的模型提供者，提高系统的可扩展性和用户体验。