# 模型重构 - 编译、运行、调试命令指南

## 概述

本文档详细说明重构后的模型相关代码的编译、运行和调试命令，确保开发人员能够正确构建和测试重构后的系统。

## 环境准备

在执行以下命令之前，请确保：

1. 已安装 Node.js 和 npm
2. 已安装项目依赖：`npm install`

## 编译命令

### 1. 编译 Electron 代码

```bash
# 编译 Electron 相关的 TypeScript 代码
npx tsc -p tsconfig.electron.json
```

### 2. 编译整个项目

```bash
# 编译整个项目的 TypeScript 代码
npx tsc
```

## 运行命令

### 1. 开发环境运行

```bash
# 启动开发服务器和 Electron 应用
npm run dev
```

### 2. 生产环境运行

```bash
# 构建生产版本
npm run build

# 运行生产版本
npm run run-prod
```

## 测试命令

```bash
# 运行测试
npm test
```

## 构建命令

### 1. 构建生产版本

```bash
# 构建生产版本
npm run build
```

### 2. 构建安装包

```bash
# 构建所有平台的安装包
npm run package

# 构建 Windows 平台的安装包
npm run package-win

# 构建 Mac 平台的安装包
npm run package-mac
```

## 调试命令

### 1. 使用 VS Code 调试

1. 在 VS Code 中打开项目
2. 选择 "Run and Debug" 选项卡
3. 选择 "Electron: Main" 配置
4. 点击 "Start Debugging" 按钮

### 2. 调试模型相关代码

1. 在模型相关代码中设置断点
2. 启动开发服务器：`npm run dev`
3. 触发相关功能，观察断点是否被触发

## 常见问题及解决方案

### 1. TypeScript 编译错误

如果遇到 TypeScript 编译错误，请检查：

- 类型定义是否正确
- 导入路径是否正确
- 方法签名是否匹配

### 2. 开发服务器启动失败

如果开发服务器启动失败，请检查：

- 端口是否被占用
- 依赖是否已正确安装
- 配置文件是否正确

### 3. 模型提供者初始化失败

如果模型提供者初始化失败，请检查：

- API 密钥是否正确
- 网络连接是否正常
- 模型提供者的 API 是否可用

## 注意事项

- 确保在执行构建命令之前，TypeScript 编译没有错误
- 在运行生产版本之前，确保已成功构建
- 在调试模型相关代码时，确保 API 密钥已正确配置

## 总结

通过本文档提供的命令，开发人员可以：

- 正确编译重构后的模型相关代码
- 在开发环境中运行和调试应用
- 构建生产版本和安装包
- 测试重构后的功能

这些命令确保了重构后的系统能够正常构建、运行和调试，为开发人员提供了便捷的开发和测试环境。